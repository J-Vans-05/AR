# AR logo — lighting fix

The logo GLB now uses a matte, non-metallic PBR material that preserves its original vertex colours. `marker-ar.html` also uses colour-managed ambient, hemisphere, key and fill lighting. `logo-ar.html` uses the neutral Model Viewer environment at a modest exposure.

Upload all files in this folder to the existing GitHub repository, replacing files with the same names. Open `marker-ar.html` for image-target AR.
